{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">

            <h1 class="titrePrincipal">Liste des équipes :</h1>

        <br>

        <table class="table table-striped table-bordered table-sm">
            <thead class="thead-dark">
            <tr>
                <th>VILLAGE</th>
                <th>PONEY</th>
                <th>IMAGE</th>
                <th>CAVALIER</th>
            </tr>
            </thead>
            <tbody>
            {% for equipe in equipes %}
                <tr>
                    <td style="vertical-align:middle">{{ equipe.village }}</td>
                    <td style="vertical-align:middle">{{ equipe.poney }}</td>
                    <td style="vertical-align:middle"><img style="width:100px" class="pony" src="{{ asset('images/'~equipe.image) }}" alt=""></td>
                    <td style="vertical-align:middle">{{ equipe.cavalier }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>

    {% include 'footer.html.twig' %}

{% endblock %}

{% block stylesheets %}

    <style>

        .titrePrincipal {
            font-family: 'Bowlby One SC', cursive;
            text-align: center;
            margin-top: 20px;
        }

        tr { text-align: center; font-size: 18px }


    </style>

{% endblock %}
