{% extends 'base.html.twig' %}

{% block body %}

	<div class="container-fluid">
		<h1>Compétition :
		</h1>

		<div class="firstCourse">



					{% for i,equipe in equipes %}
					{% set i = i +1 %}

						<div class="myProgress">
					<div class="myBar{{ i  }}">
						<span>Poney {{ equipe.poney}} avec {{ equipe.cavalier }} de {{ equipe.village }}</span><img src="images/{{ equipe.image }}"></div>
				</div>

					{% endfor %}

		</div>

		<div class="secondCourse">


			<div class="myProgress">
				<div class="myBar17"></div>
			</div>

			<div class="myProgress">
				<div class="myBar18"></div>
			</div>

			<div class="myProgress">
				<div class="myBar19"></div>
			</div>

			<div class="myProgress">
				<div class="myBar20"></div>
			</div>

			<div class="myProgress">
				<div class="myBar21"></div>
			</div>

			<div class="myProgress">
				<div class="myBar22"></div>
			</div>

			<div class="myProgress">
				<div class="myBar23"></div>
			</div>

			<div class="myProgress">
				<div class="myBar24"></div>
			</div>

		</div>
		<div class="thirdCourse">
			<div class="myProgress">
				<div class="myBar25"></div>
			</div>

			<div class="myProgress">
				<div class="myBar26"></div>
			</div>

			<div class="myProgress">
				<div class="myBar27"></div>
			</div>

			<div class="myProgress">
				<div class="myBar28"></div>
			</div>

			<div class="myProgress">
				<div class="myBar29"></div>
			</div>

		</div>

		<button onclick="start()">Voir la course</button>

	</div>
	<!-- .container -->

	<script>
		var course = 0;
		var gagnants = [];
		var participants = [];
		var gagnants2 = [];
		var participants2 = [];
		var gagnants3 = [];


		function start() {

			if (course === 0) {
				course += 1;
				for (var i = 1; i <= 16; i++) {
					move(".myBar".concat(i), i);
				}
			} else if (participants.length == 16 && course == 1) {
				player = 0;
				course += 1;
				for (var i = 17; i <= 24; i++) {

					move2(".myBar".concat(i), player);
					player += 1
				}
			} else if (participants2.length == 8 && course == 2) {
				player = 0;
				course += 1;
				for (var i = 25; i <= 29; i++) {
					move3(".myBar".concat(i), player);
						player += 1
				}
			}else if ( gagnants3.length == 3 && course == 3){
					var gagnantsFinal = 1;
				for (var i = 17; i <= 18; i++) {
					document.querySelector(".myBar".concat(i)).innerHTML = "<img src='http://res.cloudinary.com/tiffanym/image/upload/q_100/v1526633517/third_xcmjnv.png'></img><img src='images/".concat(gagnants3[2]).concat(".png'>")

				}

				for (var i = 19; i <= 21; i++) {
					document.querySelector(".myBar".concat(i)).innerHTML ="<img src='http://res.cloudinary.com/tiffanym/image/upload/q_100/v1526633517/second_qcspma.png'></img><img src='images/".concat(gagnants3[1]).concat(".png'>")
				//	document.querySelector(".myBar".concat(i)).style.width: 50 + '%'

				}
				for (var i = 22; i <= 25; i++) {
					document.querySelector(".myBar".concat(i)).innerHTML = "<img ".concat().concat(".png'>")

				}

				for (var i = 25; i <= 29; i++) {
					document.querySelector(".myBar".concat(i)).innerHTML = "<img src='http://res.cloudinary.com/tiffanym/image/upload/q_100/v1526633517/first_ymlpka.png'><img src='images/".concat(gagnants3[0]).concat(".png'>")

				}

			}

			function move(ladiv, inc) {
				var elem = document.querySelector(ladiv);
				var width = 0;
				var increment = inc;

				var id = setInterval(frame, 100);
				function frame() {
					console.log(participants);
					console.log(gagnants);
					if (participants.length == 15) {
						idpar = 0;

					/*		for (var i = 1; i <= 16; i++) {

						if (gagnants.includes(i) == true) {
								document.querySelector(".myBar".concat(i)).innerHTML = "";
							}

						} */
						for (var i = 17; i <= 24; i++) {
							document.querySelector(".myBar".concat(i)).innerHTML = "<img src='images/".concat(gagnants[idpar]).concat(".png'>")
							idpar += 1;
						}
					}

					if (width >= 100) {
						if (gagnants.length < 8) {
							gagnants.push(increment);
						}

						participants.push(increment);

						clearInterval(id);
					} else {
						width += (Math.floor(Math.random() * 1000) / 500);
						elem.style.width = width + "%";
					}
				}

			}

			function move2(ladiv, inc) {
				var elem = document.querySelector(ladiv);
				var width = 0;
				var increment = inc;
				var id = setInterval(frame, 100);

				function frame() {
					console.log(participants2);
					console.log(gagnants2);

					if (participants2.length == 7) {
						idpar = 0;
						idpar2 = 0;

						if (gagnants.length == 13) {
							gagnants.splice(0, 8);
						}

						for (var i = 17; i <= 24; i++) {
							console.log(idpar2);
				/**			if (gagnants2.includes(participants2[idpar2]) == true) {
								document.querySelector(".myBar".concat(i)).innerHTML = "";

							}*/
							idpar2 += 1;

						}
						console.log(gagnants);
						for (var i = 25; i <= 29; i++) {
							document.querySelector(".myBar".concat(i)).innerHTML = "<img src='images/".concat(gagnants2[idpar]).concat(".png'>")
								document.querySelector(".myBar".concat(i))
							idpar += 1;
						}
					}

					if (width >= 100) {
						if (gagnants2.length < 5) {
							gagnants2.push(gagnants[increment]);
						}

						participants2.push(gagnants[increment]);

						clearInterval(id);
					} else {
						width += (Math.floor(Math.random() * 1000) / 600);
						elem.style.width = width + "%";
					}
				}

			}
			function move3(ladiv, inc) {
				var elem = document.querySelector(ladiv);
				var width = 0;
				var increment = inc;
				var id = setInterval(frame, 100);
				function frame() {

					if (width >= 100) {

						if (gagnants3.length < 3) {
							gagnants3.push(gagnants2[increment]);
							console.log(gagnants3);
						}
						participants2.push(increment);
						clearInterval(id);
					} else {
						width += (Math.floor(Math.random() * 1000) / 800);
						elem.style.width = width + "%";
					}
				}

			}
		}


	</script>


{% endblock %}

{% block stylesheets %}

	<style>
		/* Reset */
		*,
		*::after,
		*::before {
			margin: 0;
			padding: 0;
			list-style-type: none;
			line-height: 1.5em;
			text-decoration: none;
			box-sizing: border-box;

		}
		/* ^_^ */
		body {
			background-color: #9fedff !important ;

			margin-top: 10em;
		}
		.container {
			max-width: 56em;
			margin: 0 auto;
			font-family: sans-serif;
			padding: 0 1em;
		}
		span {
			font-size: 1.4em;
			}
		h1 {
			color: #ddd;
			text-align: center;
			margin: 1em 0 2em;
		}
		.myProgress {
			width: 100%;

		}
		.myBar1,
		.myBar10,
		.myBar11,
		.myBar12,
		.myBar13,
		.myBar14,
		.myBar15,
		.myBar16,
		.myBar17,
		.myBar18,
		.myBar19,
		.myBar2,
		.myBar20,
		.myBar21,
		.myBar22,
		.myBar23,
		.myBar24,
		.myBar25,
		.myBar26,
		.myBar27,
		.myBar28,
		.myBar29,
		.myBar3,
		.myBar30,
		.myBar31,
		.myBar4,
		.myBar5,
		.myBar6,
		.myBar7,
		.myBar8,
		.myBar9 {
			width: 0;
			height: 1em;
			margin-bottom: 2.5em;

			color: rgba(#254986, 0);
			padding: 0.3em 0.5em;
			font-size: 1.1em;
			font-weight: bold;
			text-align: right;
			transition: 100ms ease-out;
		}
		img {
			height: 50px;
		}
		button {
			position: relative;
			padding: 0.3em 1em;
			font-size: 1em;
			font-weight: bold;
			color: #eee;
			background-color: #333;
			border-radius: 2px;
			border: none;
		}
		button:hover {
			color: #eee;
			background-color: #de3333;
			cursor: pointer;
			box-shadow: 0 5px 6px rgba(0, 0, 0, 0.4);
		}
		button:active {
			top: -1px;
		}

		hr {
			color: red;
			background-color: red;
		}

		.firstCourse {
			width: 30%;
			float: left;
		}

		.secondCourse {
			margin-top: 10%;
			margin-left: 1%;
			vertical-align: middle;
			width: 30%;
			float: left;
		}
		.thirdCourse {
			margin-top: 10%;
			margin-left: 1%;
			vertical-align: middle;
			width: 30%;
			float: left;
		}
		.fourCourse {
			margin-top: 10%;
			margin-left: 1%;
			vertical-align: middle;
			width: 20%;
			float: left;
		}
	</style>

{% endblock %}

{% block javascripts %}
<script>
		const bg = document.querySelector('.background-image');
		const windowWidth = window.innerWidth / 5;
		const windowHeight = window.innerHeight / 5 ;

		bg.addEventListener('mousemove', (e) => {
		const mouseX = e.clientX / windowWidth;
		const mouseY = e.clientY / windowHeight;

		bg.style.transform = `translate3d(-${mouseX}%, -${mouseY}%, 0)`;
		});
</script>
{% endblock %}
